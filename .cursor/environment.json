{
  "name": "Alacris",
  "install": "npm ci && npm run build && npm --prefix docs ci",
  "terminals": [
    {
      "name": "demo",
      "command": "npm run demo",
      "description": "Interactive component demo + browser test suite at http://localhost:5173/ (rebuilds the library, then serves demo/ and bench/ with a zero-dependency static server)."
    },
    {
      "name": "docs",
      "command": "npm --prefix docs run dev -- --host",
      "description": "Astro + Starlight docs site at http://localhost:4321/alacris (predev copies the freshly built bundle into docs/public/lib)."
    }
  ],
  "ports": [
    { "name": "demo", "port": 5173 },
    { "name": "docs", "port": 4321 }
  ]
}
